---
const {
  name,
  summary,
  href = "#",
} = Astro.props;
---

<a
  href={href}
  class="group block rounded-xl p-6 transition-colors duration-300"
>
  <h3 class="text-base font-semibold text-[#3a1e10]">
    {name}
  </h3>

  {summary && (
    <p class="mt-2 text-sm text-[#5a2d18]/80 leading-relaxed">
      {summary}
    </p>
  )}

  <span
    class="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-orange-700 transition-colors group-hover:text-orange-800"
  >
    Bekijk profiel →
  </span>
</a>
